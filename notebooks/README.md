# Notebooks

Original exploratory notebooks were removed from public history because their
outputs referenced private receipt data. Recreate public notebooks only with
synthetic fixtures from `examples/synthetic/`.
